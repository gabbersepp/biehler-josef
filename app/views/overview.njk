---
layout: base
pageTitle: Josef Biehler - artist, programmer, writer - Overview
---
    <div class="content overview">
        <div class="nav">
          <div class="btn clear"><a href="/index.html">Back</a></div>
        </div>

        <div class="list">

            <div class="list-item">
                <h1>Blog</h1>
                <div class="body-row">
                    {% for post in collections.postsSlice %}
                    <div class="body">
                        {% set entry = post %}
                        {% include 'devtobodyoverview.njk' %}
                    </div>
                    {% endfor %}
                </div>
                <div class="btn attention more"><a href="/dev-blog">More ...</a></div>
            </div>

            <div class="list-item">
                <h1>Projects</h1>
                <div class="body-row">
                    {% for project in collections.reposSlice %}
                    <div class="body">
                        {% set entry = project %}
                        {% include 'projectbodyoverview.njk' %}
                    </div>
                    {% endfor %}
                </div>
                <div class="btn attention more"><a href="/projects">More ...</a></div>
            </div>

            <div class="list-item">
                <h1>Drawings</h1>
                <div class="body-row">
                {% for project in collections.tweetsSlice %}
                    <div class="body">
                        {% set entry = project %}
                        {% include 'drawingsbodyoverview.njk' %}
                    </div>
                    {% endfor %}
                </div>
                <div class="btn attention more"><a href="/drawing">More ...</a></div>
            </div>

            
        </div>
    </div>